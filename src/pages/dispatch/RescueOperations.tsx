import { Users } from 'lucide-react';
import PageTemplate from '../../components/PageTemplate';

export default function RescueOperations() {
  return (
    <PageTemplate
      title="Rescue Operations"
      description="Manage and track rescue assignments"
      icon={Users}
    />
  );
}
