import { PackageX } from 'lucide-react';
import PageTemplate from '../../components/PageTemplate';

export default function RTSTracking() {
  return (
    <PageTemplate
      title="RTS Tracking"
      description="Track return-to-station packages and accountability"
      icon={PackageX}
    />
  );
}
